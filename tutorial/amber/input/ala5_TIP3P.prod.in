Production run (100ps)
&cntrl
 imin=0,
 ioutfm=1,
 iwrap=1,
 irest=0,
 ntx=1,
 ig=-1,
 ntb=1,
 cut=10, ntc=2, ntf=2,
 tempi=300.0, temp0=300.0,
 ntt=3, gamma_ln=1.0,
 nstlim=50000, dt=0.002,
 ntpr=500, ntwx=500, ntwr=500,
&end


# imin=0    no minimization
# ioutfm=1  to use netcdf binary output
# iwrap=1   to do imaging
# irest=0   do not restart the simulation
# ntx=1     load only coordinates from input file
# ig=-1     reseed random number generator

# ntb=1     constant volume

# cut=10    pme cutoffs
# ntc=2     HBond constraints
# ntf=2     HBond constraints in force evaluations

# ntt=3     Langevin integrator